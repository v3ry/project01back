import {Sequelize} from "sequelize";

const db = new Sequelize('DB_DATABASE','DB_USER','DB_PASSWORD',{
    host: "localhost",
    port: 'DB_PORT',
    // database: 'project01', /* MySQL Database */
    dialect: "mysql"
});

export default db;